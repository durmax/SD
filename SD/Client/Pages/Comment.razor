@inject ILanguageContainerService languageContainer
@inherits CommentBase

@if (commentModel != null)
{
    <div class="ml-2 mr-2">
        <div class="card mb-2">
            <div class="text-muted pl-1">
                <i class="bg-light">@note</i>
            </div>
            <div class="card-title pb-0">
                @if (loading)
                {
                    <button type="button" class="btn btn-light">
                        <div class="spinner-border spinner-border-sm" role="status">
                            <span class="sr-only">Loading...</span>
                        </div>
                    </button>
                }
                @if (commentModel.CommentId != null)
                {
                    <a type="button" class="btn btn-link"
                       href="MyWords/@commentModel.UserId/@commentModel.CommentOwnerName">@commentModel.CommentOwnerName</a>
                    <lable>  @commentModel.CreatedAt </lable>
                }
                <button type="button" style="float:right" class="btn btn-link bg-transparent @cssDelCom" @onclick="@(async () => await RemoveComment())">
                    <img width="20" height="20" src="/icons/bxs-message-x.svg" asp-append-version="true" />
                </button>
            </div>
            <div class="card-body p-2">
                <textarea @oninput="@(()=> {IsChanged=true;})" class="form-control text-wrap"
                          rows="@Rows"
                          @bind-value="@MyText"
                          @bind-value:event="onchange">@MyText </textarea>
            </div>
            @if (IsChanged)
            {
                <button type="button" class="btn btn-link bg-transparent" @onclick="@SaveComment">
                    <img width="20" height="20" src="/icons/content-save-edit-outline.svg" asp-append-version="true" /> <lable class="pl-1">@languageContainer.Keys["Save"]</lable>
                </button>
            }
        </div>
    </div>
}
